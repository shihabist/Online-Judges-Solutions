#include <bits/stdc++.h>

#define newl '\n'
#define FAST ios_base::sync_with_stdio(0)
using namespace std;

int main(){
    FAST;
    int a, b, c;
    while(cin >> a >> b >> c){
        cout << a*b*c*2 << newl;
    }
    return 0;
}
